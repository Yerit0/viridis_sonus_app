import 'package:flutter/material.dart';

class RegistroCelularScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Pagina en Construcción'),),
    );
  }
}